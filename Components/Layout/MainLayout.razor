@using GlomensioApp.Services.Accounts
@inherits LayoutComponentBase
@inject IAccountService AccountService
<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="_theme" />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />
<MudLayout>
    <MudAppBar Elevation="1">
        <MudText Typo="Typo.h5" Class="ml-3">Glomensio App</MudText>
        <MudSpacer />
        <MudToggleIconButton @bind-Toggled="@_isDarkMode"
                             Icon="@Icons.Material.Filled.LightMode" Color="@Color.Dark" Title="Off"
                             ToggledIcon="@Icons.Material.Filled.LightMode" ToggledColor="@Color.Success" ToggledTitle="On" />
        @* <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" /> *@
    </MudAppBar>
    <MudHighlighter @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
    </MudHighlighter>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>
@code {
    bool _drawerOpen = true;
    public bool _isDarkMode { get; set; } = true;
    private MudTheme _theme = new();
    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

    protected override async Task OnInitializedAsync()
    {
        // var checkLogin = await AccountService.CheckLoginAsync();
        // if (!checkLogin.Authenticated)
        // {
        //     UriHelper.NavigateTo("/login");
        // }
        // else
        // {
        //     UriHelper.NavigateTo("/");
        // }
    }

}